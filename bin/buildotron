#!/usr/bin/env perl
use v5.20;
use lib 'lib';
use Buildotron::App;

use Buildotron::Logger '$Logger' => { init => {
  ident     => 'buildotron',
  facility  => 'news',
  to_stdout => 1,
} };

Buildotron::App->run;
